def is_artist_valid(person):
    first_letter = person[0]
    


while True:
    tokens = input()
    if tokens == "end":
        break
    tokens = tokens.split(":")
    artist = tokens[0]
    song = tokens[1]
